import styled from "styled-components";

const Wrapper = styled.div`
  display: flex;
  flex-direction: column;
  text-align: center;
`;

function BeforeGoal() {
  return <Wrapper>BeforeGoal</Wrapper>;
}

export default BeforeGoal;
